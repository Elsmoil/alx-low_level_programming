#include <stdio.h>

/**
 * main - prints the number of arguments
 * @argc: int
 * @argv: list
 * Return: 0
 */

int msin(int argc, char const *argv[])
{
	int i = 0;

	while (arg--)
	{
		printf("%d\n", argv[]);
		i++;
	}
	return (0);
}
