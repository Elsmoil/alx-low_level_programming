#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
char main(void)
{	char x = 'a';
	char y = 'A';

	while
	{putchar(x <= a);
		x++;
	}
	while
	{	putchar(y <= A);
		A++;
	}
	putchar('\n')
		return (0);
}
